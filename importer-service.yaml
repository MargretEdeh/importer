apiVersion: v1
kind: Service
metadata:
  name: importer
  namespace: dev
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: importer
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
